{
  "name": "@noix/core",
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "build": "forEach-exec"
  },
  "devDependencies": {
    "single-tsc": "^0.0.1",
    "foreach-exec": "^0.0.1",
    "ts-ignore-decorator": "^0.0.1"
  }
}
